package basics;
import edu.princeton.cs.algs4.*;
/*编写一段程序，从标准输入按行读取数据，其中每行都包含一个名字和两个整数。
 * 然后用printf() 打印一张表格，每行的若干列数据包括名字、两个整数和第一
 * 个整数除以第二个整数 的结果，精确到小数点后三位。可以用这种程序将棒球球
 * 手的击球命中率或者学生的考试分数 制成表格*/
public class Exam1_1_21 {
	public static void main(String[] args) {
//		In in=new In("Se");
//		String[] whitelist=in.readAllStrings();
//		for(int i=0;i<whitelist.length;i+=3){
//			StdOut.print(whitelist[i]+" "+whitelist[i+1]+" "+whitelist[i+2]+" ");
//			double m=Double.parseDouble(whitelist[i+1]);
//			double n=Double.parseDouble(whitelist[i+2]);
//			StdOut.println(m/n);
//		}
//		for(int i=0;i<args.length;i++){
//			System.out.println(args[i]);
//		}
		String s=StdIn.readString();
		StdOut.print(s);
//		int M = 3;
//	    int index = 0;
//	    String[] strs = new String[M];
//	    while (index < M)
//	        strs[index++] = StdIn.readLine();
//	    for (int i = 0; i < strs.length; ++i) {
//	        String[] arr = strs[i].split("\\s+");//根据给定正则表达式的匹配拆分此字符串。
//	        double temp = Double.parseDouble(arr[1]) / Double.parseDouble(arr[2]);
//	        StdOut.printf("%-10s   %-10s   %-10s   %-13.3f\n", arr[0], arr[1], arr[2], temp);
//	    }

	
	}
	
//	public static void createTable(String name,double a,double b){
//		
//		
//	}

}
